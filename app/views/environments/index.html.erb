<div class="row">
  <div class="col-xs-12">
    <div class="box">

      <div class="box-header">
        <h3 class="box-title">Environments</h3>
      </div>

      <div class="box-body">

        <table class="table table-striped table-bordered">
          <thead>
            <tr>
              <th>Name</th>
              <th>Organization</th>
              <th></th>
            </tr>
          </thead>

          <tbody>
            <% @environments.each do |environment| %>
              <tr>
                <td><%= environment.name %></td>
                <td><%= environment.organization %></td>
                <td>
                  <%= link_to raw('<i class="fa fa-eye"></i>'), environment, class: 'btn btn-sm btn-default btn-flat btn-start-process' %>
                  <%= link_to raw('<i class="fa fa-pencil"></i>'), edit_organization_environment_path(environment), class: 'btn btn-sm btn-default btn-flat btn-start-process' %>
                  <%= link_to raw('<i class="fa fa-trash"></i>'), environment, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-sm btn-default btn-flat btn-start-process' %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>

      </div>

      <div class="box-footer">
        <%= link_to 'Back', organization_environments_path, class: 'btn btn-default' %>
        <%= link_to 'New Environment', new_organization_environment_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
</div>

<br>

