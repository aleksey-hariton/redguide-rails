$("#cookbooks-build").replaceWith("<%=raw escape_javascript(render partial: 'changesets/cookbook_build/cookbooks', format: 'html', locals: {cookbooks: @changeset.cookbook_builds}) %>");

$(function() {
  assignRefreshReaction();
});
