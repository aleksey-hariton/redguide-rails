$('#build-details-modal #console').html(
    "<%= escape_javascript(raw('<pre>' + CookbookBuild.find(params[:build_id]).console_out + '</pre>')) %>"
);

$('#cookbook-console-refresh i').removeClass('fa-spin')